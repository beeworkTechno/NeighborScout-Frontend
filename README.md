make sure to run this.

`
npm install react@19.1.0 react-dom@19.1.0
`

to start frontend:

`
npx expo start
`

if there is error while starting frontend try :

`
npm install
`

and retry 

`
npx expo start
`